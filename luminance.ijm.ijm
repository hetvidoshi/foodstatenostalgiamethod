// automating luminance measurements
// can easily do by hand for one image, but it takes a while for all of them

run("RGB to Luminance");//converts images to a luminance based image
run("Measure");// runs measure- this measures anything that you have clicked on Analyze>set measurements
close()